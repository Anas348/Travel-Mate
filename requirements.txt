Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Werkzeug==3.0.3
SQLAlchemy==2.0.32

# Postgres driver -- needed if DATABASE_URL starts with postgresql://
# (recommended for the live Vercel deployment, e.g. a free Neon or Supabase database)
psycopg2-binary==2.9.9

# MySQL driver -- only needed if you set DATABASE_URL to a mysql+mysqlconnector:// URL
mysql-connector-python==9.0.0
